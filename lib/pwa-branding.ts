import { TOKENS } from "@/lib/wealth-console-tokens"

/** Matches `app/layout.tsx` viewport — classic shell / default install chrome. */
export const PWA_THEME_COLOR_CLASSIC = "#6366f1"

/** Wealth Console (bento) shell — must match `TOKENS.surface` for iOS PWA status area. */
export const PWA_THEME_COLOR_CONSOLE = TOKENS.surface
